import 'package:flutter/material.dart';

class ProductItemsScreen extends StatelessWidget {
  static const routeName = 'product-items';

  const ProductItemsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
