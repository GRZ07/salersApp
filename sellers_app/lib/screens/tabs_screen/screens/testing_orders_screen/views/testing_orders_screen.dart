import 'package:flutter/material.dart';

class TestingOrdersScreen extends StatelessWidget {
  const TestingOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('testing orders'),
    );
  }
}
